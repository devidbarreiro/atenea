version: '3.8'

# Docker Compose para ENTORNO DEV
# Puerto: 8080
# URL: dev.atenea.nxhumans.com
# Código en: ~/dev/html/

services:
  db:
    image: postgres:15-alpine
    volumes:
      - dev_postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-atenea_dev}
      - POSTGRES_USER=${POSTGRES_USER:-atenea}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    networks:
      - atenea-dev-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-atenea}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

  web:
    build:
      context: ./html
      dockerfile: Dockerfile
    command: gunicorn atenea.wsgi:application --bind 0.0.0.0:8000 --workers 4 --timeout 120
    volumes:
      - ./html:/app
      - dev_static_volume:/app/static
      - dev_media_volume:/app/media
      - dev_logs_volume:/app/logs
      # Credenciales de GCS desde el servidor
      - ${GCS_CREDENTIALS_PATH:-./credentials.json}:/app/credentials.json:ro
    ports:
      - "8080:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-atenea}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-atenea_dev}
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
      - DEBUG=${DEBUG:-True}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - atenea-dev-network
    restart: always

  # Servicio para ejecutar migraciones (ejecutar manualmente)
  migrate:
    build:
      context: ./html
      dockerfile: Dockerfile
    command: python manage.py migrate --noinput
    volumes:
      - ./html:/app
      - ${GCS_CREDENTIALS_PATH:-./credentials.json}:/app/credentials.json:ro
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-atenea}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-atenea_dev}
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
    depends_on:
      db:
        condition: service_healthy
    networks:
      - atenea-dev-network
    profiles:
      - tools

  # Recolectar archivos estáticos (ejecutar manualmente)
  collectstatic:
    build:
      context: ./html
      dockerfile: Dockerfile
    command: python manage.py collectstatic --noinput
    volumes:
      - ./html:/app
      - dev_static_volume:/app/static
    env_file:
      - .env
    networks:
      - atenea-dev-network
    profiles:
      - tools

networks:
  atenea-dev-network:
    driver: bridge

volumes:
  dev_postgres_data:
  dev_static_volume:
  dev_media_volume:
  dev_logs_volume:

