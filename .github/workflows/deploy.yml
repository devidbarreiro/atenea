name: Deploy to Server

# Se ejecuta automáticamente cuando:
# - Push a dev → despliega a DEV
# - Push a demo → despliega a DEMO
# - Push a main → despliega a PROD
on:
  push:
    branches: [ dev, demo, main ]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Run Tests
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run tests
      env:
        SECRET_KEY: ${{ secrets.SECRET_KEY_CI || 'test-secret-key-for-ci' }}
        USE_SQLITE: 'True'
        DEBUG: 'False'
        ALLOWED_HOSTS: 'localhost,127.0.1'
        GCS_BUCKET_NAME: 'test-bucket'
        GCS_PROJECT_ID: 'test-project'
        GOOGLE_APPLICATION_CREDENTIALS: '/tmp/fake-credentials.json'
        HEYGEN_API_KEY: 'test-key'
        GEMINI_API_KEY: 'test-key'
      run: |
        python manage.py test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    name: Deploy to Server
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Determine environment
      id: env
      run: |
        BRANCH="${{ github.ref_name }}"
        if [ "$BRANCH" == "dev" ]; then
          echo "env=dev" >> $GITHUB_OUTPUT
        elif [ "$BRANCH" == "demo" ]; then
          echo "env=demo" >> $GITHUB_OUTPUT
        elif [ "$BRANCH" == "main" ]; then
          echo "env=prod" >> $GITHUB_OUTPUT
        else
          echo "❌ Branch desconocido: $BRANCH"
          exit 1
        fi
        echo "✅ Desplegando a: ${{ steps.env.outputs.env }}"
    
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        # Guardar la clave SSH preservando saltos de línea correctamente
        # Usar printf para preservar saltos de línea y caracteres especiales
        printf '%s\n' "${{ secrets.SSH_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        # Verificar que la clave es válida
        if ! ssh-keygen -l -f ~/.ssh/deploy_key > /dev/null 2>&1; then
          echo "❌ Error: La clave SSH no es válida"
          echo "Primeras líneas de la clave:"
          head -3 ~/.ssh/deploy_key
          exit 1
        fi
        echo "✅ Clave SSH válida"
        ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true
        chmod 644 ~/.ssh/known_hosts
    
    - name: Prepare deployment files
      run: |
        echo "📋 Verificando docker-compose para ${{ steps.env.outputs.env }}"
        if [ ! -f "docker/docker-compose.${{ steps.env.outputs.env }}.yml" ]; then
          echo "❌ Error: No se encontró docker-compose.${{ steps.env.outputs.env }}.yml"
          exit 1
        fi
        echo "✅ Archivos listos para despliegue"
    
    - name: Deploy to ${{ steps.env.outputs.env }}
      env:
        SSH_KEY: ~/.ssh/deploy_key
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_USER: ${{ secrets.SSH_USERNAME }}
        ENV: ${{ steps.env.outputs.env }}
      run: |
        ENV_DIR="$HOME/$ENV"
        echo "📁 Preparando directorio $ENV_DIR en el servidor"
        
        # Crear directorio en el servidor
        ssh -i $SSH_KEY -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST \
          "mkdir -p $ENV_DIR/html $ENV_DIR/backups"
        
        # Copiar código al servidor usando rsync (excluyendo .git y otros archivos innecesarios)
        echo "📤 Copiando código al servidor..."
        rsync -avz --delete \
          -e "ssh -i $SSH_KEY -o StrictHostKeyChecking=no" \
          --exclude='.git' \
          --exclude='__pycache__' \
          --exclude='*.pyc' \
          --exclude='.env' \
          --exclude='venv' \
          --exclude='db.sqlite3' \
          --exclude='.pytest_cache' \
          --exclude='node_modules' \
          ./ $SSH_USER@$SSH_HOST:$ENV_DIR/html/
        
        # Copiar docker-compose al servidor
        echo "📋 Copiando docker-compose.yml"
        scp -i $SSH_KEY -o StrictHostKeyChecking=no \
          docker/docker-compose.$ENV.yml \
          $SSH_USER@$SSH_HOST:$ENV_DIR/docker-compose.yml
        
        # Ejecutar despliegue en el servidor
        echo "🚀 Ejecutando despliegue en el servidor"
        ssh -i $SSH_KEY -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST \
          ENV_DIR="$ENV_DIR" ENV="$ENV" bash -s << 'DEPLOY_SCRIPT'
        set -e
        cd $ENV_DIR
        echo "🔨 Construyendo y reiniciando contenedores"
        docker compose down || true
        docker compose build --no-cache
        docker compose up -d
        echo "⏳ Esperando a que los servicios estén listos..."
        sleep 15
        echo "🔄 Ejecutando migraciones"
        docker compose run --rm migrate || echo "⚠️  Advertencia: Error en migraciones"
        echo "📦 Recolectando archivos estáticos"
        docker compose run --rm collectstatic || echo "⚠️  Advertencia: Error en collectstatic"
        echo "✅ Despliegue a $ENV completado!"
        if [ "$ENV" == "prod" ]; then
          echo "🌐 URL: https://atenea.nxhumans.com"
        else
          echo "🌐 URL: https://$ENV.atenea.nxhumans.com"
        fi
        DEPLOY_SCRIPT
